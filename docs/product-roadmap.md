# Product Roadmap — SlotDock

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 23/62 tasks complete
**Current Phase:** Phase 3 — Dashboard & Real-Time Updates (complete)

---

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** The first phase sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition — "Disponent öffnet morgens das Dashboard und sieht alle Anlieferungen sauber verteilt" — works as early as possible. Everything else builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Spediteur-Flow ist heilig.** The public booking flow must be frictionless at every stage. If in doubt, optimize for the Spediteur experience — it determines adoption.
8. **German-first UI.** All user-facing text in German. Code, comments, and variable names in English.

---

## Phase 0: Foundation & Setup

> **Goal:** A running Next.js app with Supabase connected, auth working, base layout rendering, and design system tokens configured. No features yet, but every subsequent task builds on a solid foundation.

**Reference sections — read these before starting this phase:**
- PRD: § 2. Technical Architecture (all subsections)
- PRD: § 9. Design System (all subsections)
- PRD: § 10. Auth Implementation (all subsections)
- Vision: § 5. Design Direction > Design Tokens

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR for review."

- [x] **TASK-001** — Initialize Next.js project with TypeScript, Tailwind, and App Router
  Files: `package.json`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `postcss.config.mjs`
  Notes: Run `npx create-next-app@latest slotdock --typescript --tailwind --app --src-dir --eslint`. Set up the project root structure per PRD § Repository Structure. Verify: `npm run dev` starts without errors, default Next.js page renders at localhost:3000.

- [x] **TASK-002** — Configure Tailwind with SlotDock design tokens
  Files: `tailwind.config.ts`, `src/app/globals.css`
  Notes: Implement the full Tailwind config from PRD § 9. Design System > Tailwind Configuration. Add CSS custom properties for all design tokens (colors, typography, spacing, shadows, radii) in globals.css `:root`. Import Inter and JetBrains Mono from Google Fonts. Verify: Create a test `<h1>` and `<button>` using the custom colors — they render correctly.

- [x] **TASK-003** — Install and configure Supabase client
  Files: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `.env.local.example`, `package.json`
  Notes: Install `@supabase/supabase-js` and `@supabase/ssr`. Create browser client (`createBrowserClient`) and server client (`createServerClient` with cookies). Create `.env.local.example` with all required env vars (see PRD § Technical Architecture > Environment Variables). Verify: Import both clients in a test page — no TypeScript errors.

- [x] **TASK-004** — Create Supabase database schema with initial migration
  Files: `supabase/migrations/001_initial_schema.sql`
  Notes: Create all tables from PRD § 3. Data Model: `profiles`, `warehouses`, `docks`, `dock_schedules`, `bookings`, `subscriptions`. Include all indexes from PRD § 3. Data Model > Indexes. Add the `ON DELETE CASCADE` constraints as specified. Enable RLS on all tables but don't add policies yet (next task). Verify: Run `supabase db push` — migration applies without errors.

- [x] **TASK-005** — Set up Row Level Security policies
  Files: `supabase/migrations/002_rls_policies.sql`
  Notes: Create RLS policies per PRD § 10. Auth Implementation > Role-Based Access. Key policies: profiles (users see own), warehouses (owner only), docks (owner via warehouse), bookings (owner read/update via warehouse, public insert via API route). Add a trigger to auto-create a profile row when a new auth user signs up. Verify: Test via Supabase SQL editor — unauthenticated queries return empty results.

- [x] **TASK-006** — Set up Supabase Auth with middleware
  Files: `middleware.ts`, `src/app/(auth)/callback/route.ts`, `src/app/(auth)/login/page.tsx`, `src/app/(auth)/signup/page.tsx`
  Notes: Implement auth middleware per PRD § 10. Auth Implementation > Protected Routes. Public routes: `/`, `/login`, `/signup`, `/book/*`, `/callback`. All other routes require auth. Create the `/callback` route handler to exchange auth code for session. Create minimal login and signup pages with email/password forms (styling comes in TASK-008). Verify: Sign up creates a user in Supabase Auth dashboard, login redirects to `/`, unauthenticated access to `/docks` redirects to `/login`.

- [x] **TASK-007** — Build base UI components (Button, Input, Card, Badge, Modal, Toast, Skeleton)
  Files: `src/components/ui/button.tsx`, `src/components/ui/input.tsx`, `src/components/ui/card.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/modal.tsx`, `src/components/ui/toast.tsx`, `src/components/ui/skeleton.tsx`, `src/components/ui/select.tsx`
  Notes: Implement all component specs from PRD § 9. Design System > Component Specifications. Use design tokens via Tailwind classes. Install `clsx` and `tailwind-merge` for className merging. Each component should support variants as props (e.g., Button: primary, secondary, danger; Badge: confirmed, arrived, completed, cancelled). Verify: Create a `/dev` page that renders all components in all variants — visual check against design spec.

- [x] **TASK-008** — Build dashboard layout with sidebar and header
  Files: `src/app/(dashboard)/layout.tsx`, `src/components/layout/sidebar.tsx`, `src/components/layout/header.tsx`, `src/components/layout/nav-item.tsx`
  Notes: Dashboard layout: fixed sidebar left (width 240px, collapsible on mobile), header top with warehouse name and user menu. Sidebar nav items: Dashboard (home icon), Rampen, Buchungen, Einstellungen. Use Lucide icons (`lucide-react`). Install `lucide-react`. Active nav item highlighted with `--color-primary-light` background. Responsive: sidebar collapses to hamburger menu below `md` breakpoint. Verify: Navigate between routes — sidebar highlights active item, layout is responsive.

- [x] **TASK-009** — Style auth pages (login and signup)
  Files: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/signup/page.tsx`, `src/components/features/auth/login-form.tsx`, `src/components/features/auth/signup-form.tsx`
  Notes: Centered card layout, SlotDock logo on top, clean form using UI components from TASK-007. Signup fields: Name, E-Mail, Passwort. Login fields: E-Mail, Passwort. Install `react-hook-form`, `@hookform/resolvers`, and `zod` for form handling. Add client-side validation (email format, password min 8 chars, name required). Link between login ↔ signup. Verify: Both forms render cleanly, validation errors show inline, successful signup creates user and redirects.

- [x] **TASK-010** — Install remaining core dependencies and create utility files
  Files: `package.json`, `src/lib/utils.ts`, `src/lib/constants.ts`, `src/lib/types.ts`
  Notes: Install `date-fns`, `date-fns-tz`. Create `utils.ts` with className merger (`cn` function using clsx + tailwind-merge), date formatting helpers (German locale), and slot time helpers. Create `constants.ts` with app-wide constants (SLOT_DURATIONS, BOOKING_STATUSES, MAX_ADVANCE_DAYS). Create `types.ts` with TypeScript interfaces for all entities (Warehouse, Dock, Booking, Profile, Subscription) matching the DB schema. Verify: All imports resolve, TypeScript compiles cleanly.

---

## Phase 1: Warehouse & Dock Management

> **Goal:** A warehouse owner can create their warehouse, add docks, configure time rules, and receive a shareable booking link. The foundation of all subsequent features.

**Reference sections — read these before starting this phase:**
- PRD: § 3. Data Model (Warehouses, Docks, Dock Schedules)
- PRD: § 4. API Specification (Warehouses, Docks endpoints)
- PRD: § 5. User Stories > Epic: Lager-Setup (US-001, US-002, US-003)
- PRD: § 6. Functional Requirements (FR-001, FR-002, FR-003)
- PRD: § 8. UI/UX Requirements > Screen: Lager-Setup, Screen: Rampenverwaltung

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/warehouse-and-dock-management`, commit, push, and open a PR for review."

- [x] **TASK-011** — Create warehouse API routes (CRUD)
  Files: `src/app/api/warehouses/route.ts`, `src/app/api/warehouses/[id]/route.ts`, `src/app/api/warehouses/[id]/regenerate-token/route.ts`
  Notes: Implement GET (list user's warehouses), POST (create), PATCH (update) per PRD § 4. API Specification > Warehouses. Use server Supabase client with auth check. Validate input with zod schemas. POST auto-generates `booking_token` (UUID). Regenerate-token endpoint creates new UUID and invalidates old. Return proper error codes (400 for validation, 401 for unauthorized). Verify: Test via curl or Postman — create a warehouse, fetch it, update name, regenerate token.

- [x] **TASK-012** — Create dock API routes (CRUD)
  Files: `src/app/api/warehouses/[warehouseId]/docks/route.ts`, `src/app/api/docks/[id]/route.ts`
  Notes: Implement GET (list docks for warehouse), POST (create dock), PATCH (update dock), DELETE (delete dock) per PRD § 4. DELETE must check for active bookings (status != 'cancelled') and return 409 if any exist. Enforce unique dock name per warehouse. Validate max_concurrent between 1–5. Verify: Create 3 docks, attempt to create duplicate name (expect 400), update max_concurrent, delete empty dock.

- [x] **TASK-013** — Build warehouse setup wizard (3-step flow)
  Files: `src/app/(dashboard)/warehouse/setup/page.tsx`, `src/components/features/warehouse/warehouse-form.tsx`, `src/components/features/warehouse/dock-form.tsx`
  Notes: Three-step setup per PRD § 8. UI/UX > Screen: Lager-Setup. Step 1: Warehouse details (name, address, opening/closing time, default slot duration, max advance booking days). Step 2: Add docks (inline form: name, dock type dropdown, max concurrent). Minimum 1 dock required. Step 3: Show generated booking link with copy button. Use react-hook-form with zod validation. Progress indicator at top. "Weiter"/"Zurück" buttons. After setup complete, redirect to dashboard. Verify: Complete full setup flow, verify warehouse and docks appear in Supabase DB, booking link copies to clipboard.

- [x] **TASK-014** — Add first-time redirect logic
  Files: `src/app/(dashboard)/page.tsx`, `src/hooks/use-warehouse.ts`
  Notes: Create `use-warehouse` hook that fetches the user's warehouse(s). On the dashboard page, if no warehouse exists, redirect to `/warehouse/setup`. If warehouse exists, show dashboard (placeholder content for now). Verify: New user signs up → lands on setup wizard. User with warehouse → lands on dashboard.

- [x] **TASK-015** — Build dock management page
  Files: `src/app/(dashboard)/docks/page.tsx`, `src/hooks/use-docks.ts`
  Notes: Table view of all docks per PRD § 8. UI/UX > Screen: Rampenverwaltung. Columns: Name, Typ, Max gleichzeitig, Status (active/inactive), Aktionen. "Rampe hinzufügen" button opens modal with dock form. Edit via modal. Toggle active/inactive. Delete with confirmation dialog (blocked if active bookings). Empty state: "Noch keine Rampen. Erste Rampe hinzufügen?" Verify: Add dock, edit it, toggle active, attempt delete.

- [x] **TASK-016** — Build settings page with booking link and profile
  Files: `src/app/(dashboard)/settings/page.tsx`
  Notes: Per PRD § 8. UI/UX > Screen: Einstellungen. Sections as cards: Profil (name, email, company — read from profile, editable), Buchungslink (display full URL, copy button, regenerate with confirmation modal), Benachrichtigungen (toggle for new booking email — stored in profiles table, add column via migration if needed). Subscription section placeholder (implemented in Phase 3). Verify: Update profile name, copy booking link, regenerate token confirms old link invalid.

---

## Phase 2: Public Slot Booking

> **Goal:** A carrier can open the booking link, see available time slots, and book a delivery in under 60 seconds — without login. This is the core value proposition and enables the magic moment.

**Reference sections — read these before starting this phase:**
- PRD: § 3. Data Model (Bookings)
- PRD: § 4. API Specification (Bookings Public endpoints)
- PRD: § 5. User Stories > Epic: Slot-Buchung (US-004, US-005)
- PRD: § 6. Functional Requirements (FR-004, FR-005, FR-006)
- PRD: § 8. UI/UX Requirements > Screen: Öffentliche Buchungsseite
- Vision: § 5. Design Direction (for the warm, accessible visual feel)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/public-slot-booking`, commit, push, and open a PR for review."

- [ ] **TASK-017** — Create public availability API endpoint
  Files: `src/app/api/public/book/[token]/availability/route.ts`
  Notes: GET endpoint per PRD § 4. API Specification > Bookings Public. Accepts `date` query param (YYYY-MM-DD). Validates token against warehouses table (use service role key to bypass RLS). Returns warehouse info (name, address), active docks, and generated slots. Slot generation logic: from opening_time to closing_time in increments of default_slot_duration_minutes. For each slot, check existing non-cancelled bookings for overlap. Mark each slot as available or unavailable. Respect dock_schedules overrides if they exist. Reject dates in the past or beyond max_advance_booking_days. Verify: Call with valid token and date — returns correct slots. Call with past date — returns 400. Invalid token — returns 404.

- [ ] **TASK-018** — Create public booking creation endpoint with race condition protection
  Files: `src/app/api/public/book/[token]/route.ts`
  Notes: POST endpoint per PRD § 4. API Specification > Bookings Public. Validate token, validate input with zod (carrier_company required, slot_start must be ISO 8601, dock_id must belong to warehouse). CRITICAL: Use PostgreSQL transaction with `SELECT ... FOR UPDATE` on the dock row to prevent race conditions. Within the transaction: check overlapping bookings for the dock/time, if available → insert booking, if not → rollback and return 409. Generate a confirmation_code (8-char alphanumeric). Return booking details. Verify: Create booking → success. Create overlapping booking → 409 error. Test concurrent requests (two terminals) — only one succeeds.

- [ ] **TASK-019** — Build public booking page: calendar and slot picker
  Files: `src/app/(public)/book/[token]/page.tsx`, `src/components/features/booking/public-calendar.tsx`, `src/components/features/booking/slot-picker.tsx`
  Notes: Per PRD § 8. UI/UX > Screen: Öffentliche Buchungsseite. Clean layout: warehouse name and address at top, then date selector (calendar or date picker showing available days), then slot grid below. Slot grid shows docks as columns, time slots as rows. Available slots are clickable (primary color), booked slots are grayed out. Selected slot gets highlighted border. No auth required. Invalid token shows error page: "Dieser Buchungslink ist ungültig." Loading state: skeleton grid. Use `date-fns` with German locale for date formatting. Verify: Open booking link, select date, slots load correctly, booked slots not clickable.

- [ ] **TASK-020** — Build booking form and confirmation flow
  Files: `src/components/features/booking/booking-form.tsx`
  Notes: When a slot is selected, a form appears below the slot picker. Fields per PRD § 6. FR-005: Firma (required), Kontaktname, E-Mail, Telefon, Kennzeichen, Referenznummer, Notizen (all optional). Form validation with zod. Submit calls POST endpoint. On success: show confirmation page with all booking details and confirmation code in a prominent card. On 409 conflict: show error toast "Zeitfenster nicht mehr verfügbar", refresh slots automatically. Form preserves input on network error. Verify: Complete a booking end-to-end, see confirmation page. Try booking a taken slot — see error and refreshed availability.

- [ ] **TASK-021** — Add responsive design for public booking page
  Files: `src/app/(public)/book/[token]/page.tsx`, `src/components/features/booking/public-calendar.tsx`, `src/components/features/booking/slot-picker.tsx`
  Notes: The public page MUST work well on mobile — carriers use their phones. Calendar: full-width on mobile. Slot grid: switch from column layout (docks as columns) to stacked layout on mobile (each dock as a separate section). Form: single column, large touch targets (44px min). Test at 375px width (iPhone SE). Verify: Open booking link on mobile viewport, complete full booking flow without horizontal scrolling.

---

## Phase 3: Dashboard & Real-Time Updates

> **Goal:** The warehouse dashboard shows today's deliveries on a timeline per dock, updates in real-time when new bookings arrive, and allows the dispatcher to manage booking statuses. This delivers the magic moment.

**Reference sections — read these before starting this phase:**
- PRD: § 4. API Specification (Bookings Authenticated endpoints)
- PRD: § 5. User Stories > Epic: Dashboard (US-006, US-007)
- PRD: § 6. Functional Requirements (FR-007, FR-008, FR-009)
- PRD: § 8. UI/UX Requirements > Screen: Tages-Dashboard, Screen: Buchungsübersicht
- Vision: § 5. Design Direction > Color Palette (for status colors)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/dashboard-and-realtime`, commit, push, and open a PR for review."

- [x] **TASK-022** — Create authenticated bookings API endpoints
  Files: `src/app/api/warehouses/[warehouseId]/bookings/route.ts`, `src/app/api/bookings/[id]/status/route.ts`
  Notes: GET bookings per PRD § 4. Accepts query params: `date` (required, YYYY-MM-DD), `dock_id` (optional), `status` (optional). Returns bookings sorted by slot_start. PATCH status endpoint: validate status transitions (confirmed→arrived→completed, confirmed→cancelled, confirmed→no_show). Cancelled requires `cancellation_reason` in body. Set `cancelled_at` timestamp on cancellation. Auth check: user must own the warehouse. Verify: Fetch bookings by date, update status through valid transitions, attempt invalid transition (expect 400).

- [x] **TASK-023** — Build dock timeline component
  Files: `src/components/features/dashboard/dock-timeline.tsx`, `src/components/features/dashboard/booking-card.tsx`
  Notes: Horizontal timeline for a single dock. Time axis from opening_time to closing_time. Booking cards positioned by slot_start/slot_end as proportional widths. Card shows: carrier company, time range, reference number (if set). Card color by status: confirmed=`--color-primary-light`, arrived=`--color-warning-light`, completed=`--color-success-light`, cancelled=`--color-bg` with strikethrough. Clicking a card opens a detail slide-over panel. Verify: Render with mock data — cards align correctly to time axis, colors match status.

- [x] **TASK-024** — Build day view with all docks
  Files: `src/components/features/dashboard/day-view.tsx`, `src/components/features/dashboard/stats-bar.tsx`
  Notes: Vertical stack of dock timelines, one per active dock. Date picker at top (default: today). Stats bar above timelines showing: total bookings today, by status count (confirmed/arrived/completed). Dock names as labels on the left of each timeline. Empty state: "Heute keine Buchungen. Buchungslink an Spediteure senden?" with copy button for booking link. Verify: Dashboard renders with real bookings from Phase 2 test data, date picker switches days, stats bar counts match.

- [x] **TASK-025** — Wire up dashboard page with data fetching
  Files: `src/app/(dashboard)/page.tsx`, `src/hooks/use-bookings.ts`
  Notes: Create `use-bookings` hook that fetches bookings for the selected date. Dashboard page uses day-view component. Data fetching via Supabase client. Loading state: skeleton timelines. Error state: retry button. Date changes trigger re-fetch. Verify: Dashboard shows real bookings, date picker works, loading skeleton shows briefly.

- [x] **TASK-026** — Add Supabase Realtime for live booking updates
  Files: `src/hooks/use-bookings.ts`
  Notes: Per PRD § 6. FR-008. Subscribe to `bookings` table changes (INSERT, UPDATE) filtered by warehouse_id. When a new booking arrives, add it to the local state. When a booking status changes, update it in local state. Show a subtle toast "Neue Buchung: [Firma], [Uhrzeit]" when an INSERT arrives. Handle connection loss: show info banner "Live-Updates unterbrochen" with manual refresh button. Ensure REPLICA IDENTITY FULL is set on the bookings table (add migration). Verify: Open dashboard, create a booking via the public page in another tab — booking appears within 2 seconds without refresh.

- [x] **TASK-027** — Build booking detail slide-over with status management
  Files: `src/components/features/dashboard/booking-detail.tsx`
  Notes: Slide-over panel from the right when clicking a booking card. Shows all booking fields: Firma, Kontaktname, Telefon, E-Mail, Kennzeichen, Referenznummer, Notizen, Status, Slot-Zeit. Status action buttons at bottom: "Angekommen" (if confirmed), "Abgefertigt" (if arrived), "Stornieren" (always except completed/cancelled), "Nicht erschienen" (if confirmed). Stornieren opens a small modal asking for reason (text input). Status change calls PATCH endpoint and updates UI immediately. Verify: Click booking card, slide-over opens, change status, see updated color on timeline.

- [x] **TASK-028** — Build bookings list page with filters
  Files: `src/app/(dashboard)/bookings/page.tsx`
  Notes: Per PRD § 8. UI/UX > Screen: Buchungsübersicht. Filter bar: date range picker, dock dropdown, status dropdown. Table with columns: Datum, Uhrzeit, Rampe, Firma, Referenz, Status. Click row → same booking detail slide-over. Pagination (50 per page). Sort by date descending. Empty state: "Keine Buchungen gefunden." Verify: Apply filters, results update, click row opens detail, pagination works.

---

## Phase 4: Email Notifications

> **Goal:** Carriers receive booking confirmations and dispatchers get new-booking notifications. Email is the glue that drives adoption of the booking link.

**Reference sections — read these before starting this phase:**
- PRD: § 5. User Stories > Epic: Benachrichtigungen (US-008, US-009)
- PRD: § 6. Functional Requirements (FR-010, FR-011)
- PRD: § 12. Edge Cases & Error Handling > Feature: E-Mail
- PRD: § 13. Dependencies & Integrations > Third-Party Services (Resend)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/email-notifications`, commit, push, and open a PR for review."

- [ ] **TASK-029** — Set up Resend and create email utility
  Files: `src/lib/email/client.ts`, `src/lib/email/send.ts`, `package.json`
  Notes: Install `resend`. Create email client with API key from env. Create `sendEmail` wrapper function that handles errors gracefully: on failure, log error but don't throw (booking must succeed even if email fails). Add retry logic: 3 attempts with 5-second delay. Verify: Send a test email to your own address — it arrives.

- [ ] **TASK-030** — Create email templates (booking confirmation, new booking notification)
  Files: `src/lib/email/templates/booking-confirmation.tsx`, `src/lib/email/templates/new-booking-notification.tsx`
  Notes: Use React Email style (Resend supports JSX templates). Booking confirmation for carrier: SlotDock header, "Buchungsbestätigung" title, warehouse name, dock, date, time, confirmation code, booking details. Sachlich-professionell tone per brand voice. New booking notification for dispatcher: "Neue Buchung" title, carrier company, dock, date, time, reference. Both templates: responsive, clean, use SlotDock colors. No emojis, no "Hey!" — professional and clear. Verify: Render templates with test data — output looks professional.

- [ ] **TASK-031** — Integrate email sending into booking flow
  Files: `src/app/api/public/book/[token]/route.ts`
  Notes: After successful booking creation: if carrier_email is provided, send booking confirmation email (async, don't await — booking response returns immediately). Then send new-booking notification to warehouse owner (fetch owner email from profiles via warehouse). Respect the notification toggle from settings (check profiles table). Verify: Create booking with email → confirmation arrives. Create booking without email → no confirmation, but dispatcher still gets notified.

- [ ] **TASK-032** — Add notification settings toggle functionality
  Files: `src/app/(dashboard)/settings/page.tsx`
  Notes: Wire up the notification toggle from TASK-016 to actually control email sending. Add `notify_new_bookings` boolean column to profiles table (migration, default true). Settings page toggle updates this field. Booking endpoint checks this field before sending dispatcher notification. Verify: Toggle off → no dispatcher email on new booking. Toggle on → email arrives.

---

## Phase 5: Payments & Subscription Gating

> **Goal:** Stripe subscription with trial period, plan-based feature gating (dock limits), and customer portal for self-service billing management.

**Reference sections — read these before starting this phase:**
- PRD: § 11. Payment Integration (all subsections)
- PRD: § 3. Data Model (Subscriptions table)
- PRD: § 12. Edge Cases & Error Handling > Feature: Auth & Payments

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/payments-and-subscriptions`, commit, push, and open a PR for review."

- [ ] **TASK-033** — Set up Stripe and create products/prices
  Files: `src/lib/stripe/client.ts`, `src/lib/stripe/plans.ts`, `package.json`
  Notes: Install `stripe`. Create Stripe client with secret key from env. Define plan constants per PRD § 11: Starter (3 docks, 49€/month), Professional (10 docks, 149€/month), Business (unlimited, 299€/month). Each with yearly discount (2 months free). Note: actual Stripe Price IDs must be created in the Stripe Dashboard — use placeholder strings that the developer replaces. Verify: Stripe client initializes without error.

- [ ] **TASK-034** — Create trial subscription on signup
  Files: `supabase/migrations/003_trial_on_signup.sql`, `src/app/(auth)/callback/route.ts`
  Notes: When a new user completes signup (in the auth callback), create a subscription row with status='trialing', plan='starter', current_period_end = NOW() + 14 days. This gives every new user a 14-day trial with Starter-level features. No Stripe customer creation yet — that happens when they choose a paid plan. Verify: New signup → subscription row created with trialing status and correct end date.

- [ ] **TASK-035** — Build Stripe checkout flow
  Files: `src/app/api/stripe/checkout/route.ts`, `src/app/(dashboard)/settings/page.tsx`
  Notes: POST endpoint that creates a Stripe Checkout Session per PRD § 11. Accept plan (starter/professional/business) and billing_period (monthly/yearly). Creates Stripe Customer if not exists (store stripe_customer_id in subscriptions). Redirect to Stripe Checkout. Success URL includes session_id. On settings page: pricing cards showing all 3 plans with monthly/yearly toggle, "Plan wählen" button triggers checkout. Verify: Click plan → redirected to Stripe Checkout (test mode) → complete payment → redirected back.

- [ ] **TASK-036** — Implement Stripe webhook handler
  Files: `src/app/api/webhooks/stripe/route.ts`
  Notes: Per PRD § 11. Handle events: checkout.session.completed (activate subscription), customer.subscription.updated (update plan/status), customer.subscription.deleted (mark cancelled), invoice.payment_failed (mark past_due). CRITICAL: Verify webhook signature using raw body. Use Supabase service role client to update subscriptions table. Verify: Use Stripe CLI `stripe listen --forward-to localhost:3000/api/webhooks/stripe` to test webhook flow. Complete a test checkout → subscription row updated.

- [ ] **TASK-037** — Add subscription gating (dock limits, trial expiry)
  Files: `src/hooks/use-subscription.ts`, `src/app/api/warehouses/[warehouseId]/docks/route.ts`, `src/components/features/subscription-banner.tsx`
  Notes: Create `use-subscription` hook that returns current plan, status, and limits. Dock creation endpoint: check dock count against plan limit, return 403 if exceeded. Trial expiry: if status=trialing and current_period_end < now, show banner "Ihr Testzeitraum ist abgelaufen. Plan wählen um weiterzuarbeiten." with CTA. Past-due: show warning banner "Zahlung fehlgeschlagen." Feature gating: all features read-only when expired/past_due (bookings still work for carriers!). Verify: Create docks up to limit → next attempt blocked. Set trial end to past date → banner appears.

- [ ] **TASK-038** — Add Stripe Customer Portal link
  Files: `src/app/api/stripe/portal/route.ts`, `src/app/(dashboard)/settings/page.tsx`
  Notes: POST endpoint that creates a Stripe Billing Portal session. Returns URL. On settings page subscription section: show current plan, status, next billing date. "Abo verwalten" button opens portal in new tab. Portal allows: plan change, cancellation, payment method update, invoice download. Verify: Click "Abo verwalten" → Stripe Portal opens, can change plan.

---

## Phase 6: Polish & Launch Prep

> **Goal:** Production-ready quality: comprehensive error handling, empty states, loading states, landing page, responsive design pass, accessibility audit, and SEO basics.

**Reference sections — read these before starting this phase:**
- PRD: § 7. Non-Functional Requirements (all)
- PRD: § 12. Edge Cases & Error Handling (all)
- PRD: § 8. UI/UX Requirements > Screen: Landing Page
- Vision: § 5. Design Direction > Accessibility Commitments
- Vision: § 4. Brand Strategy > Messaging Framework, Elevator Pitches

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 6. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-6/polish-and-launch-prep`, commit, push, and open a PR for review."

- [ ] **TASK-039** — Comprehensive error handling pass
  Files: All API routes, all form components
  Notes: Per PRD § 12. Edge Cases. Audit every API route for: proper error responses (400/401/403/404/409/500), consistent error format `{ error: CODE, message: string }`. Audit every form for: network error handling (show toast, preserve input), validation error display (inline under fields). Add global error boundary component for unexpected errors. Verify: Disconnect network → submit form → see friendly error. Invalid API calls → proper error codes.

- [ ] **TASK-040** — Empty states and loading states for all pages
  Files: `src/app/(dashboard)/page.tsx`, `src/app/(dashboard)/docks/page.tsx`, `src/app/(dashboard)/bookings/page.tsx`
  Notes: Per PRD § 8. UI/UX every screen has defined empty and loading states. Dashboard empty: "Heute keine Buchungen. Buchungslink an Spediteure senden?" + copy button. Docks empty: "Noch keine Rampen. Erste Rampe hinzufügen?" + add button. Bookings empty: "Keine Buchungen gefunden." Loading: skeleton screens matching content shape (use Skeleton component). Every page must have both states implemented. Verify: Delete all test data → see empty states. Throttle network → see skeletons.

- [ ] **TASK-041** — Build landing page
  Files: `src/app/page.tsx`
  Notes: Per PRD § 8. UI/UX > Screen: Landing Page. Use messaging from Vision § 4. Brand Strategy. Hero: "Schluss mit Excel und Telefonaten." + Sub: "SlotDock ist die Online-Rampenbuchung für Fulfillment-Unternehmen." + CTA "Jetzt kostenlos testen" → /signup. Feature section: 3 cards (Spediteure buchen selbst, Keine Überbuchungen, Volle Übersicht) with Lucide icons. Pricing section: 3 plan cards with monthly/yearly toggle. Footer: Impressum, Datenschutz links (placeholder pages). No stock photos, no illustrations — clean typography and icons only. Verify: Page looks professional, CTA links work, responsive on mobile.

- [ ] **TASK-042** — Create legal placeholder pages
  Files: `src/app/(public)/impressum/page.tsx`, `src/app/(public)/datenschutz/page.tsx`
  Notes: Minimal placeholder pages with basic structure. Impressum: company info placeholder. Datenschutz: DSGVO-compliant privacy policy skeleton covering: data collected (name, email, booking data), storage (Supabase EU), purpose, retention. Lasse will fill in the actual legal text. Add note at top: "ENTWURF — Bitte von einem Anwalt prüfen lassen." Verify: Pages render, accessible from landing page footer.

- [ ] **TASK-043** — Responsive design audit and fixes
  Files: All pages and components
  Notes: Test every page at 375px (mobile), 768px (tablet), 1024px (desktop), 1280px (wide). Fix issues: sidebar collapse on mobile (hamburger), dashboard timeline horizontal scroll on mobile, booking page stacked layout on mobile, tables → card layout on mobile for bookings list. Touch targets: all buttons min 44px. No horizontal overflow on any page. Verify: Complete full user journey on 375px viewport — no broken layouts, all interactive elements reachable.

- [ ] **TASK-044** — Accessibility audit and fixes
  Files: All components
  Notes: Per Vision § 5 > Accessibility Commitments (WCAG 2.1 AA). Audit: all images have alt text, all form inputs have labels, focus indicators visible on all interactive elements, heading hierarchy is correct (no skipped levels), ARIA labels on icon-only buttons, color contrast meets 4.5:1 minimum, keyboard navigation works for all flows (tab through entire booking flow). Use browser DevTools accessibility audit. Verify: Tab through entire booking flow without mouse — all steps reachable. Run Lighthouse accessibility audit → score > 90.

- [ ] **TASK-045** — SEO basics and metadata
  Files: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/(public)/book/[token]/page.tsx`
  Notes: Root layout: meta title "SlotDock — Online-Rampenbuchung für Fulfillment-Unternehmen", meta description using the 30-second pitch. Open Graph tags for social sharing. Public booking page: dynamic title "Anlieferung buchen — [Warehouse Name]". Favicon (create simple SVG logo with "SD" letters in primary blue). Robots.txt allowing crawling of landing page, blocking /dashboard. Sitemap.xml (static, just landing page for now). Verify: Share landing page URL → OG preview shows correctly. Google Lighthouse SEO score > 90.

- [ ] **TASK-046** — Performance optimization
  Files: `next.config.ts`, various components
  Notes: Per PRD § 7. Performance targets: LCP < 2s, TTI < 3s, bundle < 150KB gzipped. Optimize: lazy load non-critical components (booking detail, modals), optimize Supabase queries (select only needed columns), add proper caching headers for static assets, ensure images (if any) use next/image. Run Lighthouse performance audit. Address any score below 90. Verify: Lighthouse performance score > 90 on landing page and dashboard.

- [ ] **TASK-047** — Add toast notification system and global providers
  Files: `src/components/ui/toast-provider.tsx`, `src/app/(dashboard)/layout.tsx`
  Notes: If not already implemented: create a toast context/provider that wraps the dashboard layout. Toast component supports success, error, info variants per PRD § 9. Design System. Auto-dismiss after 4 seconds. Toasts used for: booking link copied, status changed, settings saved, errors. Verify: Trigger all toast variants — they render correctly and auto-dismiss.

---

## Phase 7: Post-Launch Iteration

> **Goal:** Placeholder tasks for Phase 2+ features based on user feedback. These are not part of the MVP but are the natural next steps once the core product is validated.

**Reference sections — read these before starting this phase:**
- PRD: § 14. Out of Scope (for feature descriptions)
- PRD: § 15. Open Questions (for decision points)
- Vision: § 3. Product Strategy > Feature Priority (MoSCoW)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 7. These are post-launch features. Only start these after the MVP is live and validated with real users. Prioritize based on user feedback."

- [ ] **TASK-048** — Booking cancellation by carrier (via confirmation code)
  Files: `src/app/(public)/book/cancel/page.tsx`, `src/app/api/public/bookings/[code]/cancel/route.ts`
  Notes: Per PRD § 15. OQ-1. Public page where carriers enter confirmation code to cancel. Deadline: 2 hours before slot start. Link included in booking confirmation email. Verify: Cancel booking via code → status changes to cancelled, dispatcher notified.

- [ ] **TASK-049** — Booking modification by carrier
  Files: `src/app/(public)/book/modify/page.tsx`, `src/app/api/public/bookings/[code]/modify/route.ts`
  Notes: Similar to cancellation — carrier enters confirmation code, sees current booking, can change to a different available slot. Atomic operation: old slot released and new slot booked in one transaction. Verify: Modify booking → old slot freed, new slot booked.

- [ ] **TASK-050** — Delivery type management with variable slot durations
  Files: `src/app/(dashboard)/settings/delivery-types/page.tsx`, `supabase/migrations/xxx_delivery_types.sql`
  Notes: Different delivery types (Palette, Container, Stückgut) with different durations. Warehouse owner defines types with names and durations. Carriers select type during booking → slot duration adjusts. Verify: Create delivery types, book with different types → slot lengths differ.

- [ ] **TASK-051** — Week view for dashboard
  Files: `src/components/features/dashboard/week-view.tsx`
  Notes: Alternative to day view. Shows 5 or 7 days at once. Each day column shows dock occupancy as a heat map or condensed timeline. Toggle between day/week view. Useful for planning ahead. Verify: Week view renders all days, clicking a day switches to day view.

- [ ] **TASK-052** — Manual ETA status updates by carriers
  Files: `src/app/(public)/book/status/[code]/page.tsx`, `supabase/migrations/xxx_eta_tracking.sql`
  Notes: Carrier clicks link in confirmation email → simple page with buttons: "Bin unterwegs", "Verspätet (ca. X Min)", "Angekommen". Adds `eta_status` and `eta_delay_minutes` fields to bookings. Dashboard shows ETA status on booking cards. Verify: Carrier updates status → dashboard shows update in real-time.

- [ ] **TASK-053** — Automatic delay notifications
  Files: `src/lib/email/templates/delay-notification.tsx`
  Notes: When a carrier reports delay, automatically notify dispatcher via email and dashboard toast. If delay > slot_duration, suggest rescheduling. Verify: Carrier reports 30min delay → dispatcher gets email and dashboard notification.

- [ ] **TASK-054** — Recurring bookings
  Files: `supabase/migrations/xxx_recurring_bookings.sql`, `src/app/api/public/book/[token]/recurring/route.ts`
  Notes: Carriers with regular deliveries can set up weekly recurring bookings. Creates booking entries for the next N weeks. Cancellable individually or as series. Verify: Create recurring booking → slots created for next 4 weeks.

- [ ] **TASK-055** — Client/shipper status view
  Files: `src/app/(public)/status/[token]/page.tsx`
  Notes: Restricted view for Auftraggeber/Versender. New token type: client_token. Shows only bookings tagged with their reference. Status and timing visible, no edit capability. Verify: Client opens status link → sees only their deliveries.

- [ ] **TASK-056** — Analytics dashboard
  Files: `src/app/(dashboard)/analytics/page.tsx`
  Notes: Dock utilization rate (% of slots booked), average wait time, peak hours, no-show rate, busiest days. Date range selector. Charts using recharts. Verify: Dashboard shows accurate stats from real booking data.

- [ ] **TASK-057** — Multi-warehouse support (UI)
  Files: `src/components/layout/warehouse-switcher.tsx`
  Notes: The data model already supports multiple warehouses per profile. Add warehouse switcher in the header. All queries filter by selected warehouse. Verify: Create second warehouse → switcher appears → switching changes all data.

- [ ] **TASK-058** — Dynamic slot rescheduling
  Files: `src/lib/scheduling/reschedule-engine.ts`
  Notes: When a delay is reported, system suggests available alternative slots. Dispatcher can approve or reject. Carrier gets notified of new time. Verify: Report delay → system suggests 2 alternatives → dispatcher approves → carrier notified.

- [ ] **TASK-059** — API for external integrations
  Files: `src/app/api/v1/`, API documentation
  Notes: REST API with API key authentication for WMS/ERP integration. Endpoints: list bookings, create booking, update status. Rate limited. API docs page. Verify: External system can create and query bookings via API key.

- [ ] **TASK-060** — GPS-based ETA tracking
  Files: `src/lib/tracking/gps-provider.ts`
  Notes: Integration with telemetry providers or a simple carrier app that shares location. Calculate ETA based on distance and traffic. Auto-update booking ETA. Verify: GPS position updates → ETA recalculated → dashboard updated.

- [ ] **TASK-061** — DSGVO data retention and cleanup
  Files: `supabase/migrations/xxx_data_retention.sql`
  Notes: Per PRD § 15. OQ-4. Automated cleanup: anonymize booking data older than 12 months (remove carrier_company, contact info, keep timestamps and dock info for analytics). Cron job or Supabase Edge Function on schedule. Verify: Old bookings anonymized, analytics still work.

- [ ] **TASK-062** — Carrier notification on dispatcher cancellation
  Files: `src/lib/email/templates/booking-cancelled.tsx`
  Notes: Per PRD § 15. OQ-6. When dispatcher cancels a booking and carrier had an email, send notification with cancellation reason and link to rebook. Verify: Dispatcher cancels → carrier gets email with reason and rebook link.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD and vision doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** The PRD and vision doc are large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/62 tasks complete` and `**Current Phase:** Phase N`.
- **Phase 7 is different:** Post-launch tasks are not sequential — prioritize based on user feedback. Each task can be a standalone PR.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md and docs/product-vision.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."

### Phase Review Workflow

After completing every phase, push the work as a pull request for external review.

**Branch naming:** `phase-{N}/{phase-slug}` (e.g., `phase-0/foundation-and-setup`)

**PR format:**
```
Title: Phase {N}: {Phase Title}

Body:
## Goal
{The phase's goal statement}

## Completed
- {X} tasks completed (TASK-{first} through TASK-{last})

## What to verify
- {Key things to test manually}

## Reference sections used
- PRD: § {sections}
- Vision: § {sections}
```

**Review agent:** Recommend [CodeRabbit](https://coderabbit.ai) for automated code review on every PR. If it flags issues, fix in follow-up commits on the same branch before merging.
